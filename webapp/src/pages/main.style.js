const StyledMenu = {
    display: 'flex',
    justifyContent: 'center'
};

const StyledSearch = {
    display: 'flex',
    justifyContent: 'center',
    marginTop: '2%',
}

const StyledImage = {
    display: 'flex',
    justifyContent: 'center',
    marginBottom: '3%',
    width: '100%',
    height: '350px'
}

const StyledTable = {
    display: 'flex',
    justifyContent: 'center',
    marginTop: '3%',
}

export {StyledMenu, StyledSearch, StyledImage, StyledTable}