const styledTable = {
    backgroundColor: 'white',
    padding: '20px 20px 20px 20px',
    borderRadius: '20px',
    marginBottom: '5%',
    width: '800px'
};

const styledTableHeader = {
    backgroundColor: 'white',
    fontFamily: 'Fredoka, sans-serif',
    fontSize: '16px',
    textAlign: 'center'
};

const styledTableBody = {
    backgroundColor: 'white',
    fontFamily: 'Fredoka, sans-serif',
    fontSize: '16px',
    textAlign: 'center'
};

const styledTableRow = {
    marginTop: '1%',
    marginBottom: '1%'
};

const styledTableImage = {
    borderRadius: '50%',
    width: '48px',
    height: '48px'
};

const moreInfoButton = {
    width: '16px',
    height: '16px',
    cursor: 'pointer',
    backgroundColor: 'white',
    border: 'none'
}

export {styledTable, styledTableHeader, styledTableBody, styledTableRow, styledTableImage, moreInfoButton}